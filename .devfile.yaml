schemaVersion: 2.1.0

metadata:
  name: test-settings

components:
  - name: tools
    container:
      image: quay.io/devfile/universal-developer-image:ubi8-latest
      memoryLimit: 2G
      env:
        - name: VSCODE_DEFAULT_WORKSPACE
          # value: $(PROJECTS_ROOT)/.code-workspace
          value: /projects/recommended-extensions-sample/workspace-file
