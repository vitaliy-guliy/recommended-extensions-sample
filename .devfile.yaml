schemaVersion: 2.1.0

metadata:
  name: test-settings

components:
  - name: tools
    container:
      # image: quay.io/devfile/universal-developer-image:ubi8-latest
      image: quay.io/vgulyy/che-code-dev:sshd
      memoryLimit: 2G
